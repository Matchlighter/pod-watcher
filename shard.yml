name: pod-watcher
version: 0.1.0

authors:
  - Ethan Knapp

crystal: 1.10.0

license: MIT

dependencies:
  kubernetes:
    github: Matchlighter/crystal-kubernetes
    # path: ../crystal-kubernetes
  netmask:
    github: plambert/netmask.cr

targets:
  pod-watcher:
    main: main.cr
